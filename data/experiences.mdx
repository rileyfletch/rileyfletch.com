import { experiences } from './experiences.js';

export const ExperienceCard = ({ experience }) => (
  <div className="experience-item group">
    <a href={`${experience.url}`}>
        <div className="flex flex-col lg:flex-row lg:items-start gap-6 p-6 rounded-lg border border-base-200 hover:border-primary/30 hover:shadow-lg transition-all duration-300">
        <div className="flex-shrink-0">
            <div className={`badge badge-${experience.badgeColor} badge-lg px-4 py-3 text-sm font-semibold`}>
            {experience.period}
            </div>
        </div>
        
        <div className="flex-grow">
            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-3">
            <h3 className="text-xl font-bold text-base-content group-hover:text-primary transition-colors">
                {experience.title}
            </h3>
            <div className={`text-lg font-medium text-${experience.companyColor}`}>
                {experience.company}
            </div>
            </div>
            <p className="text-base-content/70 leading-relaxed">
            {experience.description}
            </p>
            
            <div className="flex flex-wrap gap-2 mt-4">
            {experience.skills.map((skill) => (
                <span key={skill} className="badge badge-outline badge-sm">{skill}</span>
            ))}
            </div>
        </div>
        </div>
    </a>
  </div>
);

# Experience 👔

<div className="space-y-8">
  {experiences.map((experience) => (
    <ExperienceCard key={experience.id} experience={experience} />
  ))}
</div>